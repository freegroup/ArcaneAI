# Sensitive values — DO NOT COMMIT — copy from deployment_vars.yaml.example
# cp deployment_secrets.yaml.example deployment_secrets.yaml

telegram_bot_token: "YOUR_TELEGRAM_BOT_TOKEN"
google_api_key: "YOUR_GOOGLE_API_KEY"
openai_api_key: ""        # only needed if llm_provider or tts_provider == openai
deepseek_api_key: ""      # only needed if llm_provider == deepseek
